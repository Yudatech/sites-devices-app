{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Your assignment is to implement a small single-page application (SPA), in JavaScript with React and TypeScript, which displays sites and their devices.",
  "main": "index.js",
  "scripts": {
    "api": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "start": "json-server --watch ./db.json --port 4000 --static ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "@tanstack/react-query": "^5.51.0",
    "axios": "^1.7.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "json-server": "0.14.2",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
